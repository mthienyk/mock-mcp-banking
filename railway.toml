[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT --proxy-headers --forwarded-allow-ips='*'"
# DB setup runs at web startup (lifespan), not here: Postgres internal DNS
# is unavailable during the build/release container.
